package LeetCode.TopInterview150;

public class test {

    public static void main(String[] args) {
        Node head = new Node(null, 0);
        Node tail = head;

        push(3);
    }

    private static Node push(int value) {

    }

    private

    static class Node {
        Node next;
        int value;

        Node (Node next, int value) {
            this.next = next;
            this.value = value;
        }
    }
}
